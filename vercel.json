{
    "version": 2,
    "name": "wewe",

    "builds": [
        {
            "src": "build.js",
            "use": "@vercel/node"
          }
    ],

    "routes": [
       { "src": "/(.*)", "dest": "/" }
    ]
}